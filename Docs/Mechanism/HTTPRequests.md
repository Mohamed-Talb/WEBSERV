# HTTP REQUEST HANDLING

This module parses the raw HTTP REQUESTS from clients. It extracts the METHOD, URL PATH, HEADERS, and BODY so the server can process requests according to the configuration rules.