# WEBSERV
A lightweight HTTP/1.1 web server written in C++ that handles multiple clients, parses requests, serves static content, and supports CGI execution using non-blocking sockets and I/O multiplexing.
